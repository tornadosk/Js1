1. Для практикума из занятия 7 продумать, где можно применить замыкания.


answer :
везде, где мы использовади объект, с одним методом.

2. Не выполняя кода, ответить, что выведет браузер и почему:
if (!("a" in window)) {
    var a = 1;
}
alert(a);

answer: undefined, переменная объявлена локально внутри фигурных скобок, не существует вне условия?

var b = function a(x) {
    x && a(--x);
};
alert(a);

answer: ошибка из-за необъявленной переменной?

function a(x) {
    return x * 2;
}
var a;
alert(a);

answer:
выведется тело функции, так как в нее не было передано переменной. Хотя конечно если бы мы присвоили переменной значение, не было бы обращения к функции, мне кажется, а если бы написали alert(a(2)) результатом было бы 4.

function b(x, y, a) {
    arguments[2] = 10;
    alert(a);
}
b(1, 2, 3);
answer:
Функция из аргументов выбирает тот, что лежит под индексом [2], arguments это внутренняя(локальная) переменная, точнее массив к которому можно обратиться индексацией, 10 результат вывода.

function a() {
    alert(this);
}
a.call(null);

answer: только посредством гугла определил что window. мне кажется вы объясняли это в какой то части урока, но найти не смог, пересмотрю позже.
